#include <iostream>
#include <string>

using namespace std;

class Solution {
public:
    bool isMatch(string s, string p) {
        
    }
};

int main()
{
    string s = "aab";
    string p = "*.b";
    Solution sol;
    bool ans = sol.isMatch(s, p);
    cout << ans << endl;

    return 0;
}